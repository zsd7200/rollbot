{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "rollbot/commands",
    "title": "Rollbot's commands.json",
    "description": "Configuration for all bot commands. Anything with `basic.X` in its description will be replaced with the content of X within basic.json.",
    "type": "object",
    "properties": {
        "roll": {
            "title": "Roll",
            "description": "Roll one die or many dice.",
            "examples": [
                "$roll [count] [type of die]"
            ],
            "type": "object",
            "properties": {
                "enabled": {
                    "title": "Enabled",
                    "description": "Whether or not the command should be able to be used.",
                    "type": "boolean",
                    "default": true
                },
                "aliases": {
                    "title": "Aliases",
                    "description": "A list of all other names this command can go by.",
                    "type": "array",
                    "default": []
                },
                "description": {
                    "title": "Description",
                    "description": "A description of the command, used in the !help command.",
                    "type": "string",
                    "default": "Accepts trades initiated by the trade command."
                },
                "commandType": {
                    "title": "Command Type",
                    "description": "A definition of what type of command this is. Currently unused.",
                    "type": "string",
                    "enum": ["util", "game"],
                    "default": "util"
                },
                "cooldown": {
                    "title": "Cooldown",
                    "description": "How long (in seconds) a user should wait between uses of this command.",
                    "type": "integer",
                    "default": 0
                },
                "wait": {
                    "title": "Wait times",
                    "description": "Configurable wait times for setTimeouts used in command.",
                    "type": "object",
                    "properties": {
                        "initial": {
                            "title": "Initial",
                            "description": "First wait for when the die is rolled.",
                            "type": "integer",
                            "default": 500
                        },
                        "between": {
                            "title": "Between",
                            "description": "Wait between rolls.",
                            "type": "integer",
                            "default": 100
                        }
                    },
                    "required": ["initial", "between"]
                },
                "maxRolls": {
                    "title": "Maximum Rolls",
                    "description": "Maximum amount of rolls allowed.",
                    "type": "integer",
                    "default": 20
                },
                "maxDie": {
                    "title": "Maximum Die Faces",
                    "description": "Maximum amount of faces on the dice being rolled.",
                    "type": "integer",
                    "default": 1000
                }
            },
            "required": ["enabled", "aliases", "description", "commandType", "cooldown", "wait", "maxRolls"]
        },
        "flip": {
            "title": "Flip",
            "description": "Allows users to flip a coin.",
            "examples": [
                "$flip heads",
                "$flip tails"
            ],
            "type": "object",
            "properties": {
                "enabled": {
                    "title": "Enabled",
                    "description": "Whether or not the command should be able to be used.",
                    "type": "boolean",
                    "default": true
                },
                "aliases": {
                    "title": "Aliases",
                    "description": "A list of all other names this command can go by.",
                    "type": "array",
                    "default": []
                },
                "description": {
                    "title": "Description",
                    "description": "A description of the command, used in the !help command.",
                    "type": "string",
                    "default": "Bet basic.currency on the outcome of a coin flip."
                },
                "commandType": {
                    "title": "Command Type",
                    "description": "A definition of what type of command this is. Currently unused.",
                    "type": "string",
                    "enum": ["util", "game"],
                    "default": "game"
                },
                "cooldown": {
                    "title": "Cooldown",
                    "description": "How long (in seconds) a user should wait between uses of this command.",
                    "type": "integer",
                    "default": 4
                },
                "wait": {
                    "title": "Wait times",
                    "description": "Configurable wait times for setTimeouts used in command.",
                    "type": "object",
                    "properties": {
                        "initial": {
                            "title": "Initial",
                            "description": "First wait for when the coin is flipped.",
                            "type": "integer",
                            "default": 500
                        },
                        "result": {
                            "title": "Result",
                            "description": "Last wait before result is displayed.",
                            "type": "integer",
                            "default": 1000
                        }
                    },
                    "required": ["initial", "result"]
                }
            },
            "required": ["enabled", "aliases", "description", "commandType", "cooldown", "wait"]
        },
        "help": {
            "title": "Help",
            "description": "Shows a list of all available commands.",
            "examples": ["$help", "$?"],
            "type": "object",
            "properties": {
                "enabled": {
                    "title": "Enabled",
                    "description": "Whether or not the command should be able to be used.",
                    "type": "boolean",
                    "default": true
                },
                "aliases": {
                    "title": "Aliases",
                    "description": "A list of all other names this command can go by.",
                    "type": "array",
                    "default": ["?"]
                },
                "description": {
                    "title": "Description",
                    "description": "A description of the command, used in the !help command.",
                    "type": "string",
                    "default": "Provides help and shows all available commands."
                },
                "commandType": {
                    "title": "Command Type",
                    "description": "A definition of what type of command this is. Currently unused.",
                    "type": "string",
                    "enum": ["util", "game"],
                    "default": "util"
                },
                "cooldown": {
                    "title": "Cooldown",
                    "description": "How long (in seconds) a user should wait between uses of this command.",
                    "type": "integer",
                    "default": 0
                }
            },
            "required": ["enabled", "aliases", "description", "commandType", "cooldown"]
        }
    },
    "required": ["roll", "flip", "help"]
}
